package se.rich.grtiming2.system.manager;

import se.rich.grtiming2.system.director.JpaRaceDirector;

public class RaceEventManager extends JpaRaceDirector {
    public RaceEventManager() {
        setUp();
    }
}
